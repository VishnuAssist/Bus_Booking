import { Typography, Box } from "@mui/material";

const Rostering = () => (
  <Box>
    <Typography variant="h6">Rostering</Typography>
    <Typography color="text.secondary">Create and manage staff rosters.</Typography>
  </Box>
);

export default Rostering;
