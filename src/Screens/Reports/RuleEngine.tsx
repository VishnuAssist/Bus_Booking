import { Typography, Box } from "@mui/material";

const RuleEngine = () => (
  <Box>
    <Typography variant="h6">Rule Engine</Typography>
    <Typography color="text.secondary">Define and apply automated rules for system logic.</Typography>
  </Box>
);

export default RuleEngine;
