import { Typography, Box } from "@mui/material";

const SystemRostering = () => (
  <Box>
    <Typography variant="h6">System Rostering</Typography>
    <Typography color="text.secondary">Manage and configure system-level rostering.</Typography>
  </Box>
);

export default SystemRostering;
